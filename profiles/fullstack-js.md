# Profile — fullstack-js

Core rules plus the JavaScript/TypeScript full-stack web extensions.

**Extensions:** `nextjs`, `react`, `typescript`, `postgres`

```bash
imperator init --profile fullstack-js
```

Use this for a Next.js App Router app with a PostgreSQL database (Prisma or similar).
