# Profile — python-api

Core rules plus the Python API backend extensions.

**Extensions:** `python`, `fastapi`, `postgres`, `api-rest`

```bash
imperator init --profile python-api
```

Use this for a FastAPI service backed by PostgreSQL exposing a REST API.
