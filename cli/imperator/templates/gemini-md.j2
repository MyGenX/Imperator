# Imperator

Generated by Imperator v{{ version }} · do not edit by hand — run `imperator compile`.

This project's AI rules are managed by Imperator and organized for Gemini CLI:

- **Global rules** — imported from `.gemini/rules/global.md`.
{% if domains %}- **Domain rules** — imported from `.gemini/rules/domains/`.
  Active domains: {{ domains | join(", ") }}.
{% else %}- **Domain rules** — none active yet; add one with `imperator add <stack>`.
{% endif %}
{% if roles %}- **Roles** — role instructions in `.gemini/rules/roles/` plus custom commands in `.gemini/commands/roles/`.
  Active roles: {{ roles | join(", ") }}.
{% else %}- **Roles** — none active yet; add one with `imperator role add <role>`.
{% endif %}

@.gemini/rules/global.md
{% for d in domains %}
@.gemini/rules/domains/{{ d }}.md
{% endfor %}
