<!-- Generated by Imperator v{{ version }} · style: {{ style }} · do not edit by hand -->
# Imperator Rules — AGENTS.md

Operating rules for Codex in this repository.

This project's AI rules are managed by Imperator and organized for Codex native
loading:

- **Global rules** — always-on project instructions in this `AGENTS.md`.
- **Rule modules** — generated under `.codex/rules/` for review and reuse.
{% if domains %}- **Domain rules** — active tech-stack guidance embedded below because Codex only auto-loads `AGENTS.md`.
  Active domains: {{ domains | join(", ") }}.
{% else %}- **Domain rules** — none active yet; add one with `imperator add <stack>`.
{% endif %}
{% if roles %}- **Roles** — project custom agents in `.codex/agents/`.
  Active roles: {{ roles | join(", ") }}.
{% else %}- **Roles** — none active yet; add one with `imperator role add <role>`.
{% endif %}

Global rules: {{ global_rule_count }}.
Domain rules: {{ domain_rule_count }}.

Generated rule modules:
- `.codex/rules/global.md`
{% for d in domains %}- `.codex/rules/domains/{{ d }}.md`
{% endfor %}{% for r in roles %}- `.codex/rules/roles/{{ r }}.md`
{% endfor %}

---

# Imperator Global Rules

{{ global_block }}
{% if domain_block %}

# Active Domain Rules

{{ domain_block }}
{% endif %}
